# GitHub Pages 部署

目标仓库为 `cb9zd8brp4-ship-it/lee`。项目配置每天北京时间约 08:17、17:17 自动抓取、筛选、翻译并部署。是否已经上线，以 Actions 部署成功和在线页面可访问为准。

1. 打开已创建的公开仓库 `cb9zd8brp4-ship-it/lee`。
2. 把本项目目录的全部内容上传到仓库根目录，保留 `.github/workflows/update.yml`。
3. Settings → Pages → Source 选择 GitHub Actions。
4. Actions → Lee 信息首页每日更新 → Run workflow。
5. 发布成功后，地址为 `https://cb9zd8brp4-ship-it.github.io/lee/`。

仓库只包含网页程序和公开文章元数据，不包含收藏、摘录或笔记。工作流使用只读内容权限，Pages 发布权限仅用于部署；不会自动提交或推送用户数据。
